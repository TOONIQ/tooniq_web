---
import { industryProblems } from "@utils/tooniq-data";
import ProblemCard from "./ProblemCard.astro";
---

<section class="bg-white py-24 dark:bg-slate-800">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <!-- セクションヘッダー -->
    <div class="mb-16 text-center">
      <h2
        class="mb-4 text-4xl font-bold text-slate-900 sm:text-5xl dark:text-slate-100"
      >
        アニメ業界が抱える課題
      </h2>
      <p class="mx-auto max-w-2xl text-lg text-slate-600 dark:text-slate-300">
        TOONIQは、これらの構造的な問題を技術で解決します
      </p>
    </div>

    <!-- 問題カードグリッド -->
    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {industryProblems.map((problem) => <ProblemCard problem={problem} />)}
    </div>

    <!-- 解決策へのリンク -->
    <div class="mt-16 text-center">
      <p class="mb-6 text-xl font-semibold text-slate-900 dark:text-slate-100">
        これらの課題に、私たちのソリューションで立ち向かいます
      </p>
      <a
        href="/about"
        class="inline-flex items-center justify-center gap-2 text-lg font-semibold text-purple-600 transition-colors hover:text-purple-700 dark:text-purple-400 dark:hover:text-purple-300"
      >
        TOONIQのアプローチを見る
        <svg
          class="h-5 w-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
        </svg>
      </a>
    </div>
  </div>
</section>
