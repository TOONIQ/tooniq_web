---
interface Props {
  service: {
    id: string;
    name: string;
    subtitle: string;
    description: string;
    features: string[];
    icon: string;
  };
}

const { service } = Astro.props;

// アイコンマッピング
const iconMap: Record<string, string> = {
  calendar: "📅",
  lightbulb: "💡",
  chat: "💬",
  document: "📄",
  code: "👨‍💻",
  globe: "🌐",
};
---

<div
  class="group relative overflow-hidden rounded-2xl border border-slate-200 bg-white p-8 shadow-sm transition-all duration-300 hover:shadow-2xl hover:scale-105 dark:border-slate-700 dark:bg-slate-800"
>
  <!-- アイコン -->
  <div
    class="mb-6 inline-flex h-16 w-16 items-center justify-center rounded-xl bg-gradient-to-br from-purple-500 to-indigo-600 text-4xl shadow-lg"
  >
    {iconMap[service.icon] || "🚀"}
  </div>

  <!-- タイトル -->
  <h3
    class="mb-2 text-2xl font-bold text-slate-900 transition-colors group-hover:text-purple-600 dark:text-slate-100 dark:group-hover:text-purple-400"
  >
    {service.name}
  </h3>

  <!-- サブタイトル -->
  <p class="mb-4 text-sm font-medium text-purple-600 dark:text-purple-400">
    {service.subtitle}
  </p>

  <!-- 説明 -->
  <p class="mb-6 text-slate-600 dark:text-slate-300">
    {service.description}
  </p>

  <!-- 特徴リスト -->
  <ul class="space-y-3">
    {
      service.features.map((feature) => (
        <li class="flex items-start gap-3 text-sm text-slate-700 dark:text-slate-300">
          <span class="mt-1 text-purple-500">✓</span>
          <span>{feature}</span>
        </li>
      ))
    }
  </ul>

  <!-- ホバーエフェクト用のグラデーション -->
  <div
    class="absolute inset-0 -z-10 bg-gradient-to-br from-purple-500/5 to-indigo-600/5 opacity-0 transition-opacity duration-300 group-hover:opacity-100"
  >
  </div>
</div>
